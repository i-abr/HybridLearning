#include <stdio>
#include <iostream>

// define all constants
const float PI = 3.14;

// define functions that help you do things
float calculate_circle_area(float radius, float diameter) {
    // calculate area
    return area;
}
float calculate_square_area(float length, float width) {
    // calculate area
    return area;
}
float calculate_triangle_area(float base, float height) {
    // calculate area
    return area;
}


int main() {

    // define local variables
    // local inputs for radius, diameter, length, width, base, height
    // int selection

    // print menu, get selection using cin

    // switch statement over in menu inputs
    // if 1 -> what is the radius, and diameter of area
    //  calculate area of calculate_circle_area
    // if 2 ->
    int user_input;
    cin >> user_input;
    switch (user_input)
    {
        case 1: // if user_input is 1
            // do things
            float radius;
            cin >> radius;
            if ( radius < 0) {
                print statement here
                do things
                break;
            }
            ;
        case 2:
            // do oher things
            ;
        default: // if user_input not in the cases
            cout << "suck a dick" << endl;
    }

    return 0;
}
